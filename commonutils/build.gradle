apply from: "$rootDir/buildSrc/androidModule.gradle"

configureDeploy(this)

dependencies {
    // Jetbrains annotations
    api "org.jetbrains:annotations:$jetbrainsAnnotationsVersion"

    // Support
    api "com.android.support:support-annotations:$supportLibraryVersion"
    javadocDependencies "com.android.support:support-annotations:$supportLibraryVersion"
    api "com.android.support:palette-v7:$supportLibraryVersion"
    api "com.android.support:design:$supportLibraryVersion"
    api "com.android.support:exifinterface:$supportLibraryVersion"

    api "commons-net:commons-net:$commonsNetVersion"
    api "com.google.code.gson:gson:$gsonVersion"
}

