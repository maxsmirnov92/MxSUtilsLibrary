{

  "package": {
    "name": "MxSUtilsLibrary",
    "repo": "maven",
    "subject": "maxsmirnov92"
  },

  "version": {
    "name": "[version]"
  },

  "files": [
    {
      "includePattern": "commonutils/build/release/(.*)",
      "uploadPattern": "$1"
    },
    {
      "includePattern": "customcontentprovider/build/release/(.*)",
      "uploadPattern": "$1"
    },
    {
      "includePattern": "networkutils/build/release/(.*)",
      "uploadPattern": "$1"
    },
    {
      "includePattern": "recyclerview-lib/build/release/(.*)",
      "uploadPattern": "$1"
    },
    {
      "includePattern": "taskutils/build/release/(.*) ",
      "uploadPattern": "$1"
    }
  ],

  "publish": true

}