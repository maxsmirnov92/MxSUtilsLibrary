{

  "package": {
    "name": "MxSUtilsLibrary",
    "repo": "maven",
    "subject": "maxsmirnov92"
  },

  "version": {
    "name": "[version]"
  },

  "files": [
    {
      "includePattern": "commonutils/build/release/(.*) | customcontentprovider/build/release/(.*) | networkutils/build/release/(.*) | recyclerview-lib/build/release/(.*) | taskutils/build/release/(.*) ",
      "uploadPattern": "$1"
    }
  ],

  "publish": true

}